import { NextResponse } from 'next/server';
import { getDb } from '../../../db';
import { analyticsEvents } from '../../../db/schema';
import { hasSupabasePublicConfig } from '../../../lib/supabase/config';
import { createSupabaseServerClient } from '../../../lib/supabase/server';
export async function POST(request:Request){const input=await request.json() as {visitorId?:string;event?:string;path?:string;metadata?:unknown};if(!input.visitorId||!input.event||!input.path)return NextResponse.json({error:'Événement incomplet'},{status:400});if(hasSupabasePublicConfig()){const supabase=await createSupabaseServerClient();const {error}=await supabase!.from('analytics_events').insert({id:crypto.randomUUID(),visitor_id:String(input.visitorId).slice(0,100),event_name:String(input.event).slice(0,80),path:String(input.path).slice(0,300),metadata:input.metadata||{}});if(error)return NextResponse.json({error:'Événement non enregistré'},{status:503});return NextResponse.json({ok:true},{status:201})}await getDb().insert(analyticsEvents).values({id:crypto.randomUUID(),visitorId:String(input.visitorId).slice(0,100),eventName:String(input.event).slice(0,80),path:String(input.path).slice(0,300),metadataJson:JSON.stringify(input.metadata||{}).slice(0,4000),createdAt:new Date()});return NextResponse.json({ok:true},{status:201})}
